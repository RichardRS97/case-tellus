# Camada gerada pelo pipeline: warehouse DuckDB e cache de cambio.
# Sao artefatos reconstruiveis por `python run.py`, nao fonte de verdade.
data/*.duckdb
data/fx_ptax_cache.json

# Logs e saidas intermediarias de execucao manual (nao o relatorio final).
out/*.log
out/*.txt

# Python
__pycache__/
*.pyc
.venv/
venv/

# Editor / SO
.vscode/
.idea/
Thumbs.db
.DS_Store
